rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Total Lockdown: Deny all direct client access.
    // Buddy AI's Node.js backend uses the Admin SDK, which bypasses these rules.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
